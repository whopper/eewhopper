version: 1.3.0

#services:
#  - php:
#      version: 7.3

cde-databases:
  - testdb1
  - testmigratedb1

events:
  build:
    steps:
      - setup:
          type: script
          script:
            - composer validate --no-check-all --ansi
            - composer install
